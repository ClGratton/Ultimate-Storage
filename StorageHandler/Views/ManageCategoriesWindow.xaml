<Window x:Class="StorageHandler.Views.ManageCategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Categories" Height="400" Width="350"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">
    
    <Window.Resources>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource Str_ManageCategories}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <ListBox x:Name="CategoriesList" Grid.Row="1" DisplayMemberPath="Name" SelectionMode="Single"/>

        <StackPanel Grid.Row="2" Margin="0,15,0,0">
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewCategoryBox" Height="{DynamicResource SmallControlHeight}" VerticalContentAlignment="Center" Padding="5" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0"/>
                <Button Content="{DynamicResource Str_Add}" Width="60" Height="{DynamicResource SmallControlHeight}" Margin="10,0,0,0" Click="Add_Click" Background="{DynamicResource SelectionBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="{DynamicResource SmallCornerRadius}"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <Button Content="{DynamicResource Str_DeleteSelected}" Height="{DynamicResource SmallControlHeight}" Click="Delete_Click" Background="{DynamicResource DangerBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Margin="0,0,0,10">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="{DynamicResource SmallCornerRadius}"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="{DynamicResource Str_Close}" Height="{DynamicResource SmallControlHeight}" Click="Close_Click" Background="{DynamicResource SecondaryBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="{DynamicResource SmallCornerRadius}"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
